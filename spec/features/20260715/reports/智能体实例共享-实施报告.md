# 智能体实例共享实施报告

> 功能标识：`agent-instance-sharing`
> 任务范围：`T001-T009`
> 实现确认依据：用户明确要求使用 `fons4ai-sdd-implement` 开始全部未完成任务
> SDD 等级：`S2`
> 完成日期：2026-07-16

## 1. 实施摘要

- 已完成任务：T001-T009，包含实现、自动验证、独立 Spec/Code Review 和用户人工 Gate。
- 未完成任务：无。真实 Redis 多实例联调依赖未来下游宿主应用与部署拓扑，不属于本轮任务门禁，保留为下游接入验证项。
- 阻塞任务：无代码阻塞；治理验收项待外部角色执行。
- 实施结果：完成。
- 是否可交付完成：是；实现、自动验证、独立 Reviewer 和用户人工 Gate 均已关闭。
- 是否发布就绪：是，指当前框架制品任务门禁已通过；真实 Redis 多实例联调不作为当前框架任务的发布前置条件。
- UI 设计确认状态：不适用。
- 用户跳过设计确认：否。

## 2. 变更文件

- `fons4ai-agent/fons4ai-agent-common/src/main/java/com/fons/cloud/ai/agent/api/`：新增共享 `Agent`、请求级 `AgentRun`、状态和结构化结果契约。
- `fons4ai-agent/fons4ai-agent-common/src/main/java/com/fons/cloud/ai/agent/chat/`：下沉框架无关的请求、历史消息和最终上下文 DTO。
- `fons4ai-agent/fons4ai-agent-common/src/main/java/com/fons/cloud/ai/agent/core/`：新增精确任务句柄、版本化 Redis 租约/停止命令并重构任务管理器。
- `fons4ai-agent/fons4ai-agent-spring-ai-starter/src/main/java/com/fons/cloud/ai/agent/standard/runtime/`：新增请求级运行上下文和默认 Run。
- `fons4ai-agent/fons4ai-agent-spring-ai-starter/src/main/java/com/fons/cloud/ai/agent/standard/BaseAgent.java`：共享 Agent 生命周期、统一终态、Hook、记忆和双模式执行编排。
- `fons4ai-agent/fons4ai-agent-spring-ai-starter/src/main/java/com/fons/cloud/ai/agent/standard/react/`：ReAct/Web Search 请求态迁移和共享实例适配。
- `fons4ai-agent/fons4ai-agent-spring-ai-starter/src/main/java/com/fons/cloud/ai/agent/standard/deepresearch/`：Plan-Execute 请求级 Graph 状态、checkpoint 和线程池生命周期迁移。
- `fons4ai-agent/fons4ai-agent-spring-ai-starter/src/main/java/com/fons/cloud/ai/agent/standard/skill/`：每 Run 技能快照、授权 Registry、delegate、资源工具和流状态隔离。
- `fons4ai-agent/fons4ai-agent-common/src/test/`、`fons4ai-agent/fons4ai-agent-spring-ai-starter/src/test/`：公共契约、任务竞态、共享实例、技能安全及双模式测试。
- `spec/features/20260715/checklists/智能体实例共享-S2风险检查清单.md`：S2 风险、迁移、回滚和待验收项。
- DDL/SQL：不适用；仅变更临时 Redis 值/消息契约。

## 3. TDD 记录

| 任务 | RED | GREEN | REFACTOR |
| --- | --- | --- | --- |
| T001 | Common 契约测试首次编译缺少 `Agent/AgentRun/AgentRunResult` | `AgentContractTest` 4 项通过 | 稳定 DTO 下沉 Common，移除 Spring AI 类型 |
| T002 | 旧 TaskManager 无法表达迟到 handle、版本化租约与正常完成 | `AgentTaskManagerTest`、`AgentTaskManagerCompletionTest` 通过 | compare-and-delete、停止/完成语义分离、停止消息竞态修正 |
| T003 | 共享实例测试暴露旧 BaseAgent 请求字段和一次性执行假设 | `BaseAgentSharedInstanceTest` 7 项通过 | 统一 `finishRun`、取消先确定终态、Hook 异常隔离 |
| T004 | 子类迁移阶段生产编译暴露旧 `currentConversationId/sink` 调用 | ReAct 并发、Web Search 非流式和引用集合隔离测试通过 | 工具/Advisor 防御性复制，记忆问题只传入一次 |
| T005 | Plan 非流式端到端测试发现最终值被输出为 `Optional[AssistantMessage]` | `PlanExecuteAgentGraphTest` 8 项通过 | 显式解包 Graph 状态；共享线程池改为 Agent 关闭时释放 |
| T006 | Skills 首次编译暴露一次性字段、共享 Registry/delegate/StreamState | Skills 契约、安全、资源及端到端测试通过 | 不可变 Catalog 快照原子发布，所有授权对象每 Run 创建 |
| T007 | 静态扫描与取消竞态测试覆盖请求态、日志和租约风险 | S2 清单自动项已关闭 | 日志只保留技术标识和异常类型 |
| T008 | 不适用；属于回归与独立评审门禁 | 最终 Maven 回归、Spec Review、Code Review 和用户人工 Gate 均通过 | 门禁关闭 |
| T009 | 公共契约起初没有同步结构化调用；Plan 端到端进一步发现 Optional 收口问题 | 四类 Agent `call`、失败/拒绝、非阻塞边界、双模式一致性通过 | `call` 只复用 `completion()`，无第二执行链路 |

## 4. 验证结果

- 聚焦验证命令：`mvn.cmd -pl fons4ai-agent/fons4ai-agent-spring-ai-starter -am "-Dtest=PlanExecuteAgentGraphTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`。
- 聚焦验证结果：通过；最终 `PlanExecuteAgentGraphTest` 8 项测试通过。
- 回归验证命令：设置 `JAVA_HOME=C:\hongqy\C\Java\jdk21` 后执行 `mvn.cmd -pl fons4ai-agent/fons4ai-agent-spring-ai-starter -am test`。
- 回归验证结果：通过；Agent Common 11 个、受影响 Tool Common 1 个、Agent Starter 40 个，共 52 个测试条目，其中 51 项通过、1 项因当前 Windows 环境无符号链接创建权限而跳过，失败和错误均为 0；Reactor 6 个项目 BUILD SUCCESS。生产代码仍执行显式链接过滤和真实路径校验。
- 静态验证：Common 公共契约供应商类型扫描通过；五个共享 Agent 类请求态字段扫描通过；`git diff --check` 无 whitespace error（仅仓库 CRLF 提示）。
- 手动验证：日志调用复核未发现完整请求、提示词、工具参数或技能正文输出；命中“答案”字样的两处日志只记录轮次/异常上下文，不记录答案值。
- 未验证项：真实模型、真实 Redis 多实例停止/TTL 和宿主应用实际生产日志配置归入下游接入阶段验证，不计为本轮未完成任务或本轮已验证事实。
- 验证证据等级：自动化代码证据 L3；真实外部依赖证据不足。
- 验证过程中的非代码失败：一次 Maven 聚焦命令因 PowerShell 未引用带点号的 `-D` 参数而报未知生命周期，使用引号重跑后通过。

## 5. Evidence Bundle

| 项目 | 内容 |
| --- | --- |
| 任务来源 | `spec/features/20260715/智能体实例共享-任务规划.md` T001-T009；用户显式授权全部任务 |
| 变更范围 | Agent Common 公共契约、Starter 生命周期、ReAct/Web、Plan、Skills、Redis 临时租约和测试/报告 |
| 验证命令 | 评审问题聚焦测试通过；最终 `mvn ... -am test` 共 52 个测试条目，51 项通过、1 项环境跳过、0 failures/errors；供应商边界/请求态/日志静态扫描通过 |
| 测试说明 | 新增/扩展冷流、单次启动、并发隔离、连续复用、迟到 handle、停止消息、失败/拒绝/取消、记忆去重、技能授权、四类非流式调用 |
| AC 覆盖 | AC-001 至 AC-010 均有自动化或静态证据；真实多实例验证作为下游接入建议另列，不阻塞当前 AC 验收 |
| Review 状态 | Spec Re-Review：通过，无剩余 Spec Critical/Important；第二次 Code Re-Review：通过，注册窗口取消竞态已关闭 |
| 人工 Gate | 用户已于 2026-07-16 确认通过；公共 API、Redis 协同升级、技能权限和共享扩展点责任边界已确认 |
| 风险声明 | 自动测试使用 Mockito/可控模型替身；共享扩展必须由接入方保证线程安全或按请求创建 |

## 5.1 服务级 Evidence Matrix

不适用独立服务启动、注册和健康检查；本次交付为库/Starter。保留相关依赖证据如下：

| 证据项 | 状态 | 证据来源 | 是否阻塞发布 |
| --- | --- | --- | --- |
| 编译 | 通过 | 最终 Maven Reactor 测试包含生产/测试编译 | 否 |
| 单元测试 | 通过 | Common 11、Tool 1、Starter 40；另有 1 项符号链接环境跳过 | 否 |
| 服务启动 | 不适用 | 不新增独立服务 | 否 |
| 服务发现/路由 | 不适用 | 不新增服务边界 | 否 |
| 健康/就绪检查 | 不适用 | 由宿主应用治理 | 否 |
| 核心消息链路 | 自动替身通过 | Redis 租约/停止命令确定性单元测试 | 否；真实多实例验证在下游接入时按部署拓扑补充 |
| 外部依赖验证 | 下游接入项 | 模型、Redis、工具均使用可控替身验证框架契约；真实部署按宿主拓扑验证 | 否，不阻塞当前框架任务 |
| 运行初始化 DML/Seed | 不适用 | 无初始化数据 | 否 |
| 人工 Gate | 通过 | 用户于 2026-07-16 明确确认 | 否 |

## 6. Review 与人工 Gate

- Implementer 自检：已完成。
- Spec Review：独立复审已通过，无剩余 Spec Critical/Important；确认 `T007 -> T009 -> T008`、AC-001～AC-010 覆盖以及真实 Redis 多实例联调属于下游接入项的口径一致。
- Code Review：首轮发现 1 个 Critical 和 6 个 Important；两轮独立复审后全部阻塞项关闭，最终 Code Review Gate 通过。
- Code Review 修复摘要：增加注册窗口 cancellationRequested；ReAct 工具加入取消资源树并丢弃迟到结果；Plan checkpoint 改为 Graph `doFinally` 单次释放；资源 Resolver 绑定 Run 快照；文件列表隐藏符号链接；技能数量先限流、正文按 UTF-8 字节限制；补充竞态、远程停止、并发、记忆和协议测试。
- Critical/Important 问题：首轮问题均已形成代码或证据修复；独立 Code Reviewer 已确认全部阻塞项关闭。
- 人工 Gate 适用性：适用，原因是公共 API、Redis 临时结构和技能权限模型均发生变化。
- 人工 Gate 状态：通过；用户于 2026-07-16 明确确认。
- 可交付完成判断：是；当前框架任务全部门禁已关闭。

## 6.1 Harness 校验结果

- 校验来源：fons4ai-engineering。
- 上游版本：当前环境未提供版本信息。
- 校验命令：未执行；仓库与技能包未发现可调用的工程校验器入口。
- 校验结果：未执行，原因同上。
- 失败项：无。
- 未验证项：Harness 产物结构自动校验。
- 是否阻塞交付：否；Harness 自动校验未提供可调用入口，已有独立 Review、自动化证据和人工 Gate 不受影响。
- 下一步建议：由 Harness/CI 环境运行项目约定校验器并回填结果。

## 7. AC 覆盖

| AC | 任务 | 验证证据 |
| --- | --- | --- |
| AC-001 | T001/T003-T006 | Base/ReAct 并发测试、各专属 RunContext 与静态字段扫描 |
| AC-002 | T001/T003/T006 | 冷流每订阅新 Run、同实例连续 Skills/ReAct/Base 执行 |
| AC-003 | T001/T003-T006 | final context、Hook、工具、技能、引用按 Run 聚合 |
| AC-004 | T002/T007 | 迟到 handle、CAS 删除、版本化远程停止测试 |
| AC-005 | T002/T003/T005/T006 | 完成/失败/取消/拒绝结构化终态与幂等清理测试 |
| AC-006 | T004-T006 | ReAct 并发、Web 引用集合、Plan Graph、Skills delegate/授权隔离 |
| AC-007 | T006/T007 | read_skill 后授权、未激活拒绝、资源路径和快照隔离测试 |
| AC-008 | T003/T004 | 请求快照、ChatMemory 当前问题恰好一次测试 |
| AC-009 | T001-T007 | AgentResponse 既有生成路径、停止内容与正常完成不发送停止消息测试 |
| AC-010 | T009 | 公共 call、四类 Agent call、失败/拒绝、非阻塞快速失败和双模式结果测试 |

## 8. 代码质量复盘

- 可读性检查：是；运行态类和核心流程增加中文注释。
- 方法长度与职责检查：基本通过；Plan 图节点和 Skills Builder 仍较长，但保持既有算法边界，未在本次扩大重构。
- 命名表达力检查：是；Agent/Run/TaskHandle/CatalogSnapshot 分层明确。
- 领域建模复盘：使用 DDD-lite，将运行不变量归属 AgentRun/RunContext。
- 领域行为归属：BaseAgent 负责编排，RunContext 负责请求状态，TaskManager 负责基础设施租约。
- 应用层编排检查：是。
- 基础设施依赖边界检查：是；Common API/chat 不依赖 Spring AI/Alibaba。
- 重复逻辑检查：无新增执行循环；`call` 复用 Run completion。
- 工具复用：复用 Reactor、Redisson、Spring AI/Alibaba、Fastjson 和现有 AgentResponse。
- 已考虑三方工具：项目已引入工具库。
- 新增依赖：否；Common 仅补测试范围的 Spring Boot Test。
- 异常与日志风格检查：是；错误结果安全化，敏感内容不进入新增日志。
- 测试可读性检查：是；可控模型/Redis 替身和断言场景分离。

## 9. DDL 与数据结构状态

- 是否涉及 DDL：否。
- 执行型 DDL 草案、执行方式、执行状态、SQL 快照：均不适用。
- DDL 证据：不适用。
- 发布限制：无数据库限制；Redis 临时值格式必须同版本协同升级。

## 9.1 运行初始化 DML / Seed 状态

- 是否涉及运行初始化数据：否。
- 脚本、执行、只读复核、回滚：均不适用。
- 发布限制：无。

## 10. 数据验证结果

- 是否涉及字段映射或业务数据流转：否。
- 字段、金额、日期、状态、流水号验证：不适用。
- 敏感数据处理验证：日志静态检查和技能权限单元测试已通过；人工 Gate 已确认第三方组件生产日志级别与脱敏由宿主应用在下游接入时配置。
- 数据待确认项：无当前框架任务阻塞项；真实 Redis 多实例下的租约/停止消息行为由下游接入测试确认。

## 11. S2 门禁关闭情况

- Checklist 关闭：是；独立 Review、人工 Gate 和第三方日志责任边界均已确认，真实 Redis 联调已归类为下游接入验证建议。
- 回滚方案验证：部分；步骤已记录，未实际演练。
- 兼容性风险关闭：是；自动化、独立评审和人工 Gate 均通过。
- 安全/权限风险关闭：是；代码、单元测试和责任边界 Gate 均通过，宿主实际配置在下游接入时验证。
- 事务一致性风险关闭：无数据库事务；Redis 替身测试通过，真实环境待补验。
- 其他风险控制任务关闭：否，原因同上。

## 12. 长期知识影响

- 是否产生长期知识影响：是。
- 影响类型：技术方案、接口契约、治理规则和 Redis 临时结构。
- 影响说明：Agent 生命周期变为共享定义 + 每请求 Run；新增双模式调用、精确 TaskHandle 和每 Run 技能授权。
- 是否已在本次实现中修改知识库正文：否。
- 处理边界：知识沉淀由 `fons4ai-knowledge-summary` 在用户显式触发后处理。

## 13. 问题、风险与后续事项

- 问题：无未关闭的已知代码失败；最终回归全部通过。
- 风险：真实模型/工具尚无下游联调条件；第三方 Hook/Advisor/ToolCallback 若保存请求态会破坏共享安全；技能不可变快照会缓存受限正文，需在真实技能规模下观察内存。
- 后续事项：下游宿主应用接入后，按实际部署拓扑执行 Redis 停止/TTL/升级回滚兼容性验证，并验证宿主应用第三方日志级别；这些事项不回退当前框架任务的完成状态。

## 14. CR-002 代码质量治理实施记录（2026-07-20）

> 当前任务范围：`T010-T014`
> 实现确认依据：用户先确认“执行方案二”，随后明确回复“开始执行任务”
> 当前结论：代码与聚焦测试已修改，自动化回归受依赖环境阻塞；T010-T014 暂不勾选，独立 Review 和人工 Gate 待执行。

### 14.1 已实施变更

- T010：`BaseAgent` 的任务租约清理、供应商终态释放和 Hook 分别隔离异常；正常终态会关闭主订阅及伴随资源，暂停仍不触发用户取消语义。
- T011：事件发送按单 Run 串行并检查 `EmitResult`；首包和总耗时统一为从实际启动开始的毫秒时长；React/Skills 动态参数使用固定 `parameter` 标签并进行 XML 转义；Web 解析失败降级、搜索与提取引用统一输出，Builder 复用 `BaseAgentBuilder`。
- T012：新增包内 `PlanTaskExecutor`，从 Agent 中移出任务 Future、工具权限和引用解析；每个任务只向 Alibaba delegate 注入计划指定工具；超时和取消操作真实 `Future`，独立取消令牌阻止吞掉线程中断后的迟到写回；Builder 不再创建后立即销毁默认线程池。
- T013：全仓生产/测试引用扫描后删除 `SimpleReActAgent`、旧 `chat.RoundState`、`ChatResponseParseResult` 和失效审批配置元数据；保留公共 `ChunkResult`、`RoundMode`、`AgentResumeRequest`、`AgentTaskManager`、Redisson、`start/stream/call` 及当前响应类型。

### 14.2 TDD 与验证证据

| 任务 | 新增/扩展测试 | 当前状态 |
| --- | --- | --- |
| T010 | 清理、供应商释放和 Hook 抛错；审批暂停清理失败；并发事件 800 条无静默丢失 | 用例已写入，未获得 L3 执行证据 |
| T011 | 动态参数注入转义；Web 非法 JSON、解析器异常、搜索与提取引用；Builder 字段复用 | 用例已写入，未获得 L3 执行证据 |
| T012 | 非计划工具不可执行；同步调用吞中断后的超时取消和迟到事件抑制 | 用例已写入，未获得 L3 执行证据 |
| T013 | 源码/测试引用与配置残留扫描 | 生产/测试源码扫描通过；旧 `target/classes` 仍是历史构建输出，不作为源码残留 |

- `git diff --check`：通过，无空白错误。
- 源码残留扫描：`SimpleReActAgent`、旧 `chat.RoundState`、`ChatResponseParseResult`、`fons4ai.agent.approval.enabled` 在排除 `target` 和 `spec` 后均无结果。
- Maven 聚焦编译/回归：未通过环境门禁。项目父 POM `com.fons.cloud:fons4cloud:1.0.0` 不在本地仓库；联网解析因会向 Maven Central 暴露私有组织坐标而被安全策略拒绝。
- 离线 Maven：仍缺 `spring-ai-alibaba-bom:1.1.2.0`、Spring Boot/Cloud BOM；本地仅有 Alibaba `1.1.0.0`，无法编译当前使用 Skills/并行工具 API 的源码。
- 本地 `javac` 探测：确认本地 `1.1.0.0` API 与当前分支不匹配，不能作为当前源码的有效编译结论。
- 证据等级：L2（源码、测试、静态扫描）；缺少 L3 编译/测试运行证据，因此不声明任务完成或发布就绪。

### 14.3 Review、Gate 与后续

- Implementer 自检：已完成静态复核；未替代独立 Reviewer。
- Spec Review：待执行。
- Code Review：待执行。
- 人工 Gate：待执行。
- T014：保持未完成；获得可信依赖后需执行聚焦测试、`mvn -pl fons4ai-agent/fons4ai-agent-spring-ai-starter -am test`、公共 API/Redis/事件协议扫描，再进入独立 Review 和用户 Gate。
- 长期知识：本轮未修改知识库正文；如需沉淀，由用户后续显式触发 `fons4ai-knowledge-summary`。

### 14.4 T013 补充验证与人工 Gate（2026-08-25）

- 实现确认依据：用户明确要求“继续测试T013”。
- Test Decision：T013 不改变可观察行为，复用 Common/Starter 既有契约与回归测试，不新增或修改测试代码。
- 残留扫描：排除 `target` 与 `spec` 后，`SimpleReActAgent`、旧 `chat.RoundState`、`ChatResponseParseResult`、`fons4ai.agent.approval.enabled` 均无结果；`AgentResumeRequest`、`AgentTaskManager`、Redisson 及 `start/stream/call` 公共入口仍保留。
- L3 验证：临时向 Surefire 测试类路径追加本地 `fastjson2:2.0.58` 后，受影响 Reactor 6/6 模块构建成功；Common 15/15、Tool Common 1/1、Starter 101/101 测试通过，失败、跳过均为 0。
- 已知依赖风险：标准依赖树仍将 `fastjson2-extension:2.0.58` 与 `fastjson2:2.0.15` 组合，直接运行标准测试命令会因缺少 `com.alibaba.fastjson2.util.Wrapper` 失败；该问题不由 T013 删除引入，且修复需要扩大到 Framework 依赖治理范围。
- 人工 Gate：用户于 2026-08-25 明确决定“直接打勾即可 不影响”，接受上述依赖风险不阻塞 T013；据此将 T013 标记完成，T014 状态不变。

### 14.5 T014 完整回归门禁执行记录（2026-08-25）

- 实现确认依据：用户明确要求“继续任务T014”。
- Test Decision：`BehaviorChange=false`、`TestAction=reuse`、`TestLevel=module/contract`、`RegressionScope=full`；T014 是最终发布/合并门禁，复用 Common、Tool Common 和 Starter 全部既有测试，不新增或修改测试代码。
- 标准命令：`mvn -pl fons4ai-agent/fons4ai-agent-spring-ai-starter -am test` 返回 `BUILD SUCCESS`，6/6 Reactor 模块成功，但父构建解析 `maven-surefire-plugin:2.17`，Common、Tool Common、Starter 均显示 `Tests run: 0`；该结果只能证明编译成功，不能作为自动化回归通过证据。
- 补充回归证据：显式使用 `maven-surefire-plugin:3.5.4` 并向测试类路径追加本地 `fastjson2:2.0.58` 后，Common 15/15、Tool Common 1/1、Starter 101/101 通过，合计 117 项，0 failures、0 errors、0 skipped；该命令证明当前代码行为通过，但依赖和测试插件均非标准构建解析结果。
- 静态检查：旧 Agent/类型/失效配置残留扫描通过；公共 API、Redis 版本化租约/停止命令、compare-and-delete 和文本/思考/错误/引用/推荐/停止协议路径均保留。
- `git diff --check`：未通过；当前工作树大量文件以 CRLF 行尾呈现，被 Git 报告为 trailing whitespace。未在 T014 中批量格式化或回退用户工作树。
- Spec Review：历史最终 Review 于 2026-07-16 通过，但早于 CR-002 的 T010-T013 最终源码，不能作为 T014 新鲜独立 Review。
- Code Review：历史第二次 Review 于 2026-07-16 通过，但同样未覆盖 CR-002 最终源码；当前会话 Implementer 自检不能替代独立 Reviewer。
- 人工 Gate：T013 已有用户风险接受决策；T014 的完整回归、独立 Review 和最终交付 Gate 尚未关闭。
- 当前状态：T014 保持未完成，不声明 CR-002 可交付完成。
- 失败签名：`VERIFY|T014|mvn-standard-test|Surefire-2.17-runs-0-tests`；分类为构建/任务门禁缺口。`VERIFY|T014|git-diff-check|CRLF-trailing-whitespace`；分类为工作区保护门禁。`REVIEW|T014|independent-review|CR-002-final-source-not-reviewed`；分类为外部 Gate。
- 恢复建议：通过 `fons4ai-sdd-change` 新增或授权构建依赖治理范围，统一 Surefire/JUnit 5 与 Fastjson2 版本；明确当前 CRLF 工作树的行尾处置方式；随后基于最终源码重新执行标准全量回归和独立 Spec/Code Review。

### 14.6 T014 显式命令补充验证（2026-08-26）

- 用户决策：不向 POM 增加 Surefire 插件或依赖，改用 Maven 命令行显式指定插件版本继续验证与关闭流程。
- 执行命令：`mvn -pl fons4ai-agent/fons4ai-agent-spring-ai-starter -am test-compile org.apache.maven.plugins:maven-surefire-plugin:3.5.4:test -Dmaven.test.additionalClasspath=/mnt/c/hongqy/tool/apache-maven-3.9.8/repository/com/alibaba/fastjson2/fastjson2/2.0.58/fastjson2-2.0.58.jar`；该方式未修改项目 POM。
- L3 回归结果：Reactor 6/6 模块 `BUILD SUCCESS`；Common 15、Tool Common 1、Starter 101，合计 117 项测试实际执行，0 failures、0 errors、0 skipped。
- 残留扫描：排除 `target` 后，`SimpleReActAgent`、旧 `chat.RoundState`、`ChatResponseParseResult`、`fons4ai.agent.approval.enabled` 均无结果。
- 格式检查：`git diff --ignore-space-at-eol --check` 仍因当前工作树既有 CRLF 行尾报告 `trailing whitespace`；未批量转换行尾，避免改写或覆盖用户已有变更。该结果不属于 Java 行为回归失败。
- 自动化 Gate：通过；此前 `Surefire-2.17-runs-0-tests` 已由用户选定的显式命令方案关闭，未引入构建依赖变更。
- 人工 Gate：用户已明确要求采用显式命令验证并继续推进任务关闭。
- 独立 Review：仍待覆盖 CR-002 最终源码；历史 2026-07-16 Review 不能替代本次新鲜 Review，Implementer 不自行签署独立评审。
- 当前状态：自动化与用户 Gate 已具备证据；T014 暂不勾选，仅剩独立 Spec/Code Review 门禁。

### 14.7 T014 独立评审结论（2026-08-26）

- 评审授权：用户明确回复“允许启动独立评审”。本轮分别执行独立 Spec Review 与 Code Review，评审过程只读，未修改实现。
- Spec Review：`BLOCKED`；无 Critical。Important 为任务与治理产物状态不一致：T010-T012 仍未勾选，T013/T014 的依赖链无法闭合；技术设计仍标记 CR-002 待实现；S2 风险清单仍引用 2026-07-16 的旧 Review 与 52 项测试，未覆盖 CR-002。
- Code Review：`BLOCKED`；无 Critical，存在 2 项 Important。
  - `PlanTaskExecutor` 波次超时后仅取消当前 Future 并返回失败结果，没有向运行上下文传播关闭/超时状态；`PlanExecuteAgent` 只依据 `ctx.isClose()` 停止，剩余 `pendingOrders` 仍可能继续调度。这违反技术设计 §13.4“超时或取消后禁止后继波次调度”，且现有测试只有单任务单波场景。
  - `PlanExecuteAgent` 仍直接创建 `StateGraph` 并注册节点/边；`PlanExecuteGraph` 只保存状态键和初始状态，未满足 T012“Agent 不再直接承载 Graph 构建”的 Done 条件。
- 通过项：未发现公共 API、Redis 版本化租约/停止命令或流式事件协议被 CR-002 改写；BaseAgent 终态、异常隔离、资源释放、事件串行、XML 转义、Web 降级/引用、Plan 单工具权限和真实 Future 取消等既有路径未发现新的 Critical/Important。
- `PLAN_CONFLICT`：T012 的正式 Done 条件与当前最终源码不一致；117 项回归全绿未覆盖多波次超时缺口，不能据此宣称 T012/T014 完成。
- 当前状态：T014 保持未完成；用户人工 Gate 已记录，但独立 Review 未通过。需先回到 T012 修复/补测并同步任务规划、技术设计状态和 S2 清单，然后重新执行 T014 回归与独立 Review。
- DDL/DML/服务运行：不适用；本轮未涉及关系数据库结构、初始化数据或独立可运行服务。
- 长期知识影响：暂不沉淀；待代码与评审门禁关闭后，由用户按需显式触发知识汇总。

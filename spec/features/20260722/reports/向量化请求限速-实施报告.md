# 向量化请求限速实施报告

> 功能标识：`embedding-request-rate-limit`
> 任务范围：`T001,T002,T003`
> 实现确认依据：用户最新消息“开始执行任务”
> SDD 等级：`S2`
> 完成日期：2026-07-22

## 1. 实施摘要

- 已完成任务：T001、T002、T003。
- 未完成任务：无。
- 阻塞任务：无。
- 实施结果：实现候选完成。
- 是否可交付完成：否，当前会话未完成独立 Spec Review 和 Code Review。
- 是否发布就绪：否，需完成独立评审，并由接入方按实际向量服务配额设置间隔。
- UI 设计确认状态：不适用。
- 用户跳过设计确认：否。

## 2. 变更文件

- `fons4ai-rag/fons4ai-rag-spring-ai-starter/src/main/java/com/fons/cloud/ai/rag/infrastructure/config/VectorConfigProperties.java`：新增最小请求间隔配置并保留原两参数构造方式。
- `fons4ai-rag/fons4ai-rag-spring-ai-starter/src/main/java/com/fons/cloud/ai/rag/embed/support/EmbeddingRequestRateLimiter.java`：新增单实例共享固定间隔限速器。
- `fons4ai-rag/fons4ai-rag-spring-ai-starter/src/main/java/com/fons/cloud/ai/rag/embed/support/RateLimitedEmbeddingModel.java`：新增向量模型限速装饰器。
- `fons4ai-rag/fons4ai-rag-spring-ai-starter/src/main/java/com/fons/cloud/ai/rag/embed/support/DynamicPgVectorStoreFactory.java`：在工厂生命周期内创建并复用限速模型。
- `fons4ai-rag/fons4ai-rag-spring-ai-starter/src/test/java/com/fons/cloud/ai/rag/embed/support/EmbeddingRequestRateLimiterTest.java`：新增配置、时间线、并发、中断和兼容测试。
- `fons4ai-rag/fons4ai-rag-spring-ai-starter/src/test/java/com/fons/cloud/ai/rag/embed/support/RateLimitedEmbeddingModelTest.java`：新增装饰器调用路径与异常直传测试。
- `fons4ai-rag/fons4ai-rag-spring-ai-starter/src/test/java/com/fons/cloud/ai/rag/embed/support/DynamicPgVectorStoreFactoryTest.java`：新增工厂共享范围和非法配置测试。
- `fons4ai-rag/fons4ai-rag-spring-ai-starter/src/test/java/com/fons/cloud/ai/rag/embed/support/DocumentCountBatchingStrategyTest.java`：补充分批顺序和失败零写入断言。
- 上一阶段已存在且本次继续验证：`DocumentCountBatchingStrategy.java`、`PgVectorStoreEmbeddingService.java`、`PgVectorStoreEmbeddingServiceTest.java`。

## 3. TDD 记录

| 任务 | RED | GREEN | REFACTOR |
| --- | --- | --- | --- |
| T001 | `EmbeddingRequestRateLimiterTest`：5 个用例按预期因占位实现失败 | 5 个用例通过；后续扩充至 7 个用例 | 使用单调时钟、循环重检、可中断等待；生产锁不覆盖模型网络调用 |
| T002 | `RateLimitedEmbeddingModelTest,DynamicPgVectorStoreFactoryTest`：6 个用例按预期失败 | 6 个用例通过 | 装饰器只实现必要入口；工厂只创建一份共享限速状态，不注册额外 Bean |
| T003 | 回归任务不引入新业务分支，直接补充分批顺序和兼容证据 | 聚焦测试 14/14、最终全量测试 20/20 通过 | 保留旧两参数配置构造器，避免新增字段造成调用兼容回归 |

环境修正记录：首次 Maven 在沙箱内因依赖网络受限失败；放开网络后发现机器 `JAVA_HOME` 指向 JDK 8，测试命令局部切换到已安装的 JDK 21。另一次配置绑定测试使用了当前 Spring Boot 不支持的无参 `BindResult.orElseThrow()`，已改为带异常供应器形式后通过；以上均不是产品代码失败。

## 4. 验证结果

- 聚焦验证命令：`mvn.cmd -pl fons4ai-rag/fons4ai-rag-spring-ai-starter -am '-Dtest=EmbeddingRequestRateLimiterTest,RateLimitedEmbeddingModelTest,DynamicPgVectorStoreFactoryTest,DocumentCountBatchingStrategyTest,PgVectorStoreEmbeddingServiceTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`（命令进程使用 JDK 21）。
- 聚焦验证结果：14 个测试通过，0 失败，0 错误。
- 配置与限速器复核：`EmbeddingRequestRateLimiterTest` 7 个测试通过，覆盖配置绑定、默认关闭、旧构造器兼容、顺序间隔、8 线程共享、中断和非法配置。
- 回归验证命令：`mvn.cmd -pl fons4ai-rag/fons4ai-rag-spring-ai-starter -am test`（命令进程使用 JDK 21）。
- 回归验证结果：最终状态 20 个测试通过，0 失败，0 错误；其中 RAG Starter 19 个、依赖模块 1 个。
- 手动验证：不适用，时间行为使用可控单调时钟自动验证。
- 未验证项：真实向量服务配额下的长时间压力测试未执行；不影响代码 AC，但实际间隔需由接入方按配额配置。
- 验证证据等级：L3。

## 5. Evidence Bundle

| 项目 | 内容 |
| --- | --- |
| 任务来源 | `spec/features/20260722/向量化请求限速-任务规划.md`；T001、T002、T003；用户授权“开始执行任务” |
| 变更范围 | RAG Starter 的配置、进程内限速器、模型装饰器、工厂接入和聚焦测试 |
| 验证命令 | 聚焦测试 14/14 通过；最终 Maven 全量回归 20/20 通过 |
| 测试说明 | 新增 13 个限速相关测试，并增强既有分批/零写入测试；无真实模型网络调用 |
| AC 覆盖 | AC-001 至 AC-005 均由配置、限速、并发、分批、异常和零写入测试覆盖 |
| Review 状态 | Spec Review：待执行；Code Review：待执行 |
| 人工 Gate | 不适用；不涉及权限、资金、敏感数据、DDL 或生产写入 |
| 风险声明 | 单实例边界不协调多实例配额；模型客户端内部重试不可逐次观察；配置过小仍可能 429 |

## 5.1 服务级 Evidence Matrix

不适用，本次修改的是 Spring Boot Starter 内部组件，不新增独立可运行服务或对外服务链路。

## 6. Review 与人工 Gate

- Implementer 自检：已完成。
- Spec Review：待执行，当前会话没有独立评审者。
- Spec Review 结论摘要：Implementer 自检确认实现未超出 REQ-001 至 REQ-004，独立结论待补。
- Code Review：待执行，当前会话没有独立评审者。
- Code Review 结论摘要：Implementer 已检查并发、异常、兼容、日志和测试边界，独立结论待补。
- Critical/Important 问题：Implementer 自检未发现；独立评审待执行。
- 人工 Gate 适用性：不适用。
- 人工 Gate 状态：不适用。
- 可交付完成判断：否，当前状态限定为“实现候选完成”。

## 6.1 Harness 校验结果

- 校验来源：Fons4AI SDD 严格校验器。
- 上游版本：本机已安装技能版本。
- 校验命令：`python .../validate_sdd_artifacts.py --feature-dir spec/features/20260722 --strict`。
- 校验结果：通过，输出 `OK: spec/features/20260722 SDD artifacts are valid`。
- 失败项：无。
- 未验证项：无。
- 是否阻塞交付：否；独立 Spec Review 和 Code Review 仍是交付门禁。
- 下一步建议：完成独立评审并按实际模型配额配置请求间隔。

## 7. AC 覆盖

| AC | 任务 | 验证证据 |
| --- | --- | --- |
| AC-001 | T001、T002、T003 | 默认 `0ms` 无等待、配置绑定、旧构造方式兼容测试 |
| AC-002 | T001、T002、T003 | 顺序调用精确补足间隔，模型委托时间分别为 0 和 3ms |
| AC-003 | T001、T002、T003 | 8 个并发调用共享 7 次间隔推进；工厂只持有一个装饰器 |
| AC-004 | T003 | 20 文档维持 9/9/2，文档文本顺序保持不变 |
| AC-005 | T002、T003 | 429 异常只委托一次并原样传播；第三批失败时 JdbcTemplate 零交互 |

## 8. 代码质量复盘

- 可读性、方法长度、命名表达力检查：通过。
- 领域建模复盘：不适用，本次是基础设施支持能力，不引入伪业务领域对象。
- 领域行为归属：限速规则归属基础设施支持类；应用服务继续只负责编排。
- 应用层编排检查：通过，`embedAndStore` 仍一次委托完整文档集合。
- 基础设施依赖边界检查：通过，限速器和装饰器均位于 embedding support 包。
- 重复逻辑检查：无重复。
- 工具复用：JDK 标准库 `System.nanoTime`、`TimeUnit`、同步原语和 Spring AI 既有接口。
- 已考虑三方工具：未引入 Resilience4j/Guava，符合最小改造设计。
- 新增依赖：否。
- 异常与日志风格检查：通过；中断恢复标志，不新增文档正文、向量或凭证日志。
- 测试可读性检查：通过；时间测试不使用真实秒级休眠。

## 9. DDL 与数据结构状态

- 是否涉及 DDL：否。
- 执行型 DDL 草案、执行方式、执行状态、SQL 当前结构快照和 DDL 证据：均不适用。
- 发布限制：无数据结构限制。

## 9.1 运行初始化 DML / Seed 状态

- 是否涉及运行初始化数据：否。
- DML/Seed 脚本、执行、只读复核和回滚：均不适用。
- 发布限制：无。

## 10. 数据验证结果

- 是否涉及字段映射或数据流转：否，本次只改变请求启动时序。
- 字段映射、样例输入、金额、日期、状态、流水号和客户标识验证：均不适用。
- 敏感数据处理验证：通过代码审查确认不新增敏感日志。
- 数据待确认项：无。

## 11. S2 门禁关闭情况

- Checklist 关闭：是，T003 风险与回归要求均有自动化证据。
- 回滚方案验证：是，可移除工厂装饰器恢复直接使用原始模型；无数据回滚。
- 兼容性风险关闭：是，默认关闭、旧配置构造器、公共调用方式和依赖均保持兼容。
- 安全/权限风险关闭：不适用，不改变权限或敏感数据处理。
- 事务一致性风险关闭：是，第三批模型失败时数据库零写入测试通过。
- 其他风险控制任务关闭：是，并发共享、中断、429 不重试、多实例边界均已验证或记录。

## 12. 长期知识影响

- 是否产生长期知识影响：是。
- 影响类型：技术方案、配置与运行语义。
- 影响说明：RAG 能力新增单实例共享的向量请求最小间隔配置与运行链路。
- 是否已在本次实现中修改知识库正文：否。
- 处理边界：知识沉淀由 `fons4ai-knowledge-summary` 在用户显式触发后处理，本报告不生成知识同步任务或知识汇总交接任务。

## 13. 问题、风险与后续事项

- 问题：无实现阻塞。
- 风险：配置值不是供应商配额的自动推导；多实例聚合请求和原始模型内部重试不受本限速器统一协调。
- 后续事项：接入方按模型 RPM 配额配置 `sys.rag.vector.embedding.min-request-interval-ms`；建议从 `ceil(60000 / RPM)` 毫秒起步并结合其他调用方留出余量。完成独立 Spec Review、Code Review 后再判断可交付完成。

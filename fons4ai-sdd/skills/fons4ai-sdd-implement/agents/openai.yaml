interface:
  display_name: "Fons4AI SDD Implement"
  short_description: "Routing-gated Fons4AI SDD implementation that requires explicit execution approval."
  default_prompt: "Use $fons4ai-sdd-implement only when Fons4AI routing is enabled or explicitly requested and the latest user message confirms implementation; execute all unfinished tasks unless task IDs are specified."
